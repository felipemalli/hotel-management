# Regras de sessão (carregado automaticamente — leia antes de qualquer coisa)

Há skills em `.claude/skills/`. Leia o `SKILL.md` correspondente **antes** de agir:

| Skill | Quando ler |
| --- | --- |
| `testing-frontend` | Adicionar, mover ou depurar `*.test.ts(x)` ou specs em `e2e/` |
| `frontend-ui-components` | Criar ou editar qualquer `.tsx` |
| `adding-shadcn-component` | Vendorizar um primitivo shadcn em `src/components/ui/` |
| `lean-comments` | Criar ou editar código |

Não invente o procedimento: o `SKILL.md` da pasta é a fonte.
