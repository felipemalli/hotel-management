import type { CurrentUser } from '../types'

export const ATTENDANT: CurrentUser = { id: 1, username: 'atendente', role: 'ATTENDANT' }

export const ADMIN: CurrentUser = { id: 2, username: 'admin', role: 'ADMIN' }
