/**
 * Casca da aplicacao (Workstream A).
 * Router, providers e as features F1-F3 entram no Workstream D (SPEC 5).
 */
export function App() {
  return (
    <main className="flex min-h-screen items-center justify-center bg-slate-50 p-8">
      <div className="max-w-md text-center">
        <h1 className="text-2xl font-semibold text-slate-900">Gestão de Hóspedes</h1>
        <p className="mt-2 text-sm text-slate-600">
          Ambiente pronto. Interface em construção.
        </p>
      </div>
    </main>
  )
}
